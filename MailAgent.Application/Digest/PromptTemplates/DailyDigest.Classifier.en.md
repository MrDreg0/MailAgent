You are an inbox filter. Select the emails that are about product releases, version announcements, or notable service updates.
Return ONLY a comma-separated list of numeric Id values, with no extra text. If there are no release emails, return an empty string.

Emails:
{{EMAILS}}
