services:
  mailagent-api:
    build:
      context: .
      dockerfile: MailAgent.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Database: Host=postgres;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      
      Llm__Provider: ollama
      Llm__BaseUrl: http://host.docker.internal:11434/
      Llm__FastModel: llama3.2:3b
      Llm__MainModel: qwen2.5:7b-instruct
      
      MailImport__Enabled: "true"
      MailImport__Folders__0: "Releases"
      
      MailServer__Provider: ${MAIL_PROVIDER}
      MailServer__Username: ${MAIL_USERNAME}
      MailServer__Password: ${MAIL_PASSWORD}
      
      MailServer__Imap__Host: ${MAIL_IMAP_HOST}
      MailServer__Imap__Port: ${MAIL_IMAP_PORT}
      MailServer__Imap__Security: ${MAIL_IMAP_SECURITY}
      
      MailServer__Ews__Url: ${MAIL_EWS_URL}
      MailServer__Ews__Domain: ${MAIL_EWS_DOMAIN}
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    restart: always

  mailagent-web:
    build:
      context: .
      dockerfile: MailAgent.Web/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      UseHttpsRedirection: "false"
      ConnectionStrings__Database: Host=postgres;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
    depends_on:
      - postgres
    ports:
      - "8081:8080"
    restart: always

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql
    ports:
      - "5432:5432"
    restart: always

volumes:
  postgres-data:
